services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - FUGLE_API_KEY=${FUGLE_API_KEY}

  frontend:
    build: ./frontend
    ports:
      - "4321:4321"
    depends_on:
      - backend